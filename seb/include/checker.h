#ifndef _CHECKER
#define _CHECKER

/**
 * Check the char* string has balanced parenthesis, bracket, etc.
 * Return 1 if true, 0 otherwise (and display on stderr, the error).
 */
int checker(char* string);

#endif
